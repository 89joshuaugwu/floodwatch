import {createUserWithEmailAndPassword,signInWithEmailAndPassword,signOut} from "firebase/auth"; import {doc,setDoc} from "firebase/firestore"; import {getAuthClient,getDbClient} from "@/lib/firebase";
export const login=(email:string,password:string)=>signInWithEmailAndPassword(getAuthClient(),email,password); export const logout=()=>signOut(getAuthClient());
export async function signUpResident(email:string,password:string){const c=await createUserWithEmailAndPassword(getAuthClient(),email,password);await setDoc(doc(getDbClient(),"users",c.user.uid),{uid:c.user.uid,email,role:"resident",subscribedStationIds:[]});return c}
