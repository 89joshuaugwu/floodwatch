import type {Severity,Thresholds} from "@/types"; export default function LevelGauge({level=0,thresholds,severity="normal",small=false}:{level?:number;thresholds:Thresholds;severity?:Severity;small?:boolean}){const percent=Math.min(100,Math.max(0,(level/Math.max(thresholds.dangerCm*1.2,1))*100));return <div className="gauge" aria-label={`Water level ${level} centimetres, ${severity}`} style={small?{height:104,width:48}:undefined}><div className="water" style={{height:`${percent}%`,background:`var(--${severity})`}}/><small>{level}cm</small></div>}
