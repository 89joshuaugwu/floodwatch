import Link from "next/link"; import type {Station} from "@/types"; export default function StationMap({stations}:{stations:Station[]}){return <div className="map" aria-label="Map showing flood monitoring stations">{stations.map((s,i)=><Link aria-label={s.name} href={`/stations/${s.id}`} key={s.id} className="pin" style={{left:`${25+i*26}%`,top:`${60-i*18}%`,background:`var(--${s.severity??"normal"})`}}/>)}</div>}
