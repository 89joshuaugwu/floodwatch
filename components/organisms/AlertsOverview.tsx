import type {FloodAlert} from "@/types";import SeverityBadge from "@/components/ui/SeverityBadge";export default function AlertsOverview({alerts}:{alerts:FloodAlert[]}){return <div>{alerts.length?alerts.map(a=><div className="alert row" key={a.id}><div><b>{a.stationName??a.stationId}</b><p className="muted">{a.cause==="rising_trend"?"Fast rising trend":"Threshold crossed"} · {a.triggeredAt}</p></div><SeverityBadge severity={a.severity}/></div>):<div className="empty">No alerts triggered — all stations normal</div>}</div>}
