import type {Severity} from "@/types"; export default function SeverityBadge({severity}:{severity:Severity}){const text=severity[0].toUpperCase()+severity.slice(1);return <span className={`badge ${severity}`}><i className="dot"/>{text}</span>}
