"use client";import {useState} from "react";export default function Thresholds(){const [saved,setSaved]=useState(false);return <main className="shell"><form className="card form" onSubmit={e=>{e.preventDefault();setSaved(true)}}><p className="eyebrow">Administrator</p><h1 style={{fontFamily:"Outfit"}}>Station thresholds</h1><p className="muted">Set values validated for this specific location.</p><label className="field">Watch level (cm)<input type="number" required/></label><label className="field">Warning level (cm)<input type="number" required/></label><label className="field">Danger level (cm)<input type="number" required/></label><label className="field">Rate-of-rise threshold (cm/hour)<input type="number" step="0.1" required/></label><button className="button">Save thresholds</button>{saved&&<p className="normal" style={{padding:10}}>Thresholds saved.</p>}</form></main>}
