import {demoAlerts} from "@/lib/demo";import SeverityBadge from "@/components/ui/SeverityBadge";export default function Alerts(){return <main className="shell section"><p className="eyebrow">Administrator</p><h1 style={{fontFamily:"Outfit"}}>Triggered alerts</h1><section className="card">{demoAlerts.map(a=><div className="alert row" key={a.id}><div><b>{a.stationName}</b><p className="muted">{a.cause==="rising_trend"?"Fast rising trend":"Threshold crossed"} · {a.triggeredAt}</p></div><div style={{display:"flex",gap:10,alignItems:"center"}}><SeverityBadge severity={a.severity}/><button className="button secondary">Acknowledge</button><button className="button outline">Resolve</button></div></div>)}</section></main>}
