import PublicShell from "@/components/shells/PublicShell";import StationCard from "@/components/molecules/StationCard";import {demoAlerts,demoStations} from "@/lib/demo";import SeverityBadge from "@/components/ui/SeverityBadge";
export default function Dashboard(){return <PublicShell><section className="shell section"><p className="eyebrow">Resident dashboard</p><h1 style={{font:"600 40px Outfit",margin:"8px 0 25px"}}>Your station alerts</h1><h2>Subscribed stations</h2><div className="grid station-grid">{demoStations.slice(0,2).map(s=><StationCard key={s.id} station={s}/>)}</div><h2 style={{marginTop:35}}>Alert history</h2><section className="card">{demoAlerts.map(a=><div className="alert row" key={a.id}><div><b>{a.stationName}</b><p className="muted">{a.cause==="rising_trend"?"Fast rising trend detected":"Threshold crossed"} · {a.triggeredAt}</p></div><SeverityBadge severity={a.severity}/></div>)}</section></section></PublicShell>}
