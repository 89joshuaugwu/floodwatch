import Link from "next/link"; import PublicShell from "@/components/shells/PublicShell"; import StationCard from "@/components/molecules/StationCard";import {demoStations} from "@/lib/demo";
export default function Home(){return <PublicShell><div className="shell hero"><div><p className="eyebrow">Public safety information</p><h1>Know the water level before it reaches you.</h1><p className="lead">Live, local station monitoring with clear four-stage flood alerts. Check status freely; create an account only if you want tailored updates.</p><div style={{display:"flex",gap:12,marginTop:24}}><Link className="button" href="/stations">View monitoring stations</Link><Link className="button secondary" href="/auth/signup">Get station alerts</Link></div></div><aside className="card"><p className="eyebrow">Live snapshot</p>{demoStations.map(s=><div className="stat" key={s.id}><div className="row"><b>{s.name}</b><span className={`badge ${s.severity}`}>{s.currentLevel}cm</span></div><p className="muted">{s.riverName} · updated {s.updatedAt}</p></div>)}</aside></div><section className="shell section"><div className="row"><h2>Monitoring stations</h2><Link className="muted" href="/stations">View all →</Link></div><div className="grid station-grid">{demoStations.map(s=><StationCard key={s.id} station={s}/>)}</div></section></PublicShell>}
