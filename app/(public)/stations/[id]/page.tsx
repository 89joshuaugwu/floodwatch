import {notFound} from "next/navigation";import PublicShell from "@/components/shells/PublicShell";import StationDetailView from "@/components/organisms/StationDetailView";import {demoAlerts,demoStations} from "@/lib/demo";
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;const station=demoStations.find(s=>s.id===id);if(!station)notFound();return <PublicShell><StationDetailView station={station} alerts={demoAlerts.filter(a=>a.stationId===id)}/></PublicShell>}
