import PublicShell from "@/components/shells/PublicShell";import StationMap from "@/components/organisms/StationMap";import StationCard from "@/components/molecules/StationCard";import {demoStations} from "@/lib/demo";
export default function Stations(){return <PublicShell><section className="shell section"><p className="eyebrow">Public monitoring</p><h1 style={{fontFamily:"Outfit",fontSize:42,margin:"8px 0"}}>Flood monitoring stations</h1><p className="lead">Current sensor readings are available without an account.</p><StationMap stations={demoStations}/><div className="grid station-grid" style={{marginTop:20}}>{demoStations.map(s=><StationCard key={s.id} station={s}/>)}</div></section></PublicShell>}
