// Declare framework dependencies here so PlatformIO discovers their include paths.
#include <WiFi.h>
#include <WiFiClientSecure.h>
#include <HTTPClient.h>
// The single source of firmware behavior is the standalone online sketch.
#include "../sketch.ino"
